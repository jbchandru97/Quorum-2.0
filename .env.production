# Committed on purpose: the Convex deployment URL is public — it is
# shipped to every browser by NEXT_PUBLIC_ anyway. Secrets never go
# here; CONTEXT_DEV_API_KEY lives only in .env.local and in the
# deployment platform's secret store.
NEXT_PUBLIC_CONVEX_URL=https://reminiscent-caiman-439.convex.cloud
